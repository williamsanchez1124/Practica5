<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.amda8.practica51">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="com.google.android.providers.gdf.permission.READ_GSERVICES" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_LOCATION_EXTRA_COMMANDS" />
    <uses-permission android:name="android.permission.READ_LOGS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Splash"
            android:label="@string/title_activity_splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".Maps_parque"
            android:label="@string/title_activity_maps_parque" />
        <activity
            android:name=".MapsMuelle"
            android:label="@string/title_activity_maps_muelle" />
        <activity
            android:name=".Maps_puente"
            android:label="@string/title_activity_maps_puente" />
        <activity
            android:name=".Maps_carnaval"
            android:label="@string/title_activity_maps_carnaval" />
        <activity
            android:name=".Maps_jalisco"
            android:label="@string/title_activity_maps_jalisco" />
        <activity
            android:name=".Maps_barra"
            android:label="@string/title_activity_maps_barra" />
        <activity
            android:name=".Maps_malecon"
            android:label="@string/title_activity_maps_malecon" />
        <activity
            android:name=".Maps_central"
            android:label="@string/title_activity_maps_central" />
        <activity
            android:name=".Maps_palmas"
            android:label="@string/title_activity_maps_palmas" />
        <activity
            android:name=".Maps_PuertoBerrio"
            android:label="@string/title_activity_maps__puerto_berrio" />
    </application>

</manifest>
